services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Qw852369
      POSTGRES_DB: next_pizza_main
    ports:
      - '5432:5432'
    volumes:
      - next_pizza_main_pg:/var/lib/postgresql/data

volumes:
  next_pizza_main_pg:
